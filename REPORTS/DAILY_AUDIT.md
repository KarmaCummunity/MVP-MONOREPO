# 🛡️ Daily Audit Report: 2026-07-13

## 🚨 Critical Vulnerabilities
* **Committed development JWT secret** - File: `apps/api/railway.toml.dev:33-34` -> Action: Remove the literal secret from source, rotate the development JWT secret, and document this file as a placeholder-only template. Even development secrets become reusable credentials once committed.
* **Chat API allows unauthenticated private conversation access and message impersonation** - File: `apps/api/src/controllers/chat.controller.ts:45-46`, `apps/api/src/controllers/chat.controller.ts:267-269`, `apps/api/src/controllers/chat.controller.ts:378-388`, `apps/api/src/controllers/chat.controller.ts:441-465` -> Action: Replace class-level `OptionalAuthGuard` with mandatory JWT auth for private chat routes, derive sender/viewer identity from `req.user.userId`, and enforce participant membership on every read/write.
* **Post likes/comments trust client-supplied `user_id` despite JWT auth** - File: `apps/api/src/controllers/posts.controller.ts:1343-1353`, `apps/api/src/controllers/posts.controller.ts:1573-1584`, `apps/api/src/controllers/posts.controller.ts:1975-2013`, `apps/api/src/controllers/posts.controller.ts:2053-2085`, `apps/api/src/controllers/posts.controller.ts:2152-2166` -> Action: Bind all post interaction mutations to `getSessionUserId(req)` and reject any body/query user id mismatch with 403. Current behavior lets authenticated user A like, comment, edit, or delete as user B by submitting B's UUID.
* **CRM and community-member data are exposed without route guards** - File: `apps/api/src/controllers/crm.controller.ts:38-39`, `apps/api/src/controllers/crm.controller.ts:106-150`, `apps/api/src/controllers/community-members.controller.ts:48-49`, `apps/api/src/controllers/community-members.controller.ts:186-236` -> Action: Add the appropriate admin/operator guard before exposing names, capabilities, referrers, and `contact_info`; derive `created_by` from the authenticated principal.
* **Rate-limit control endpoints are registered in production without guards** - File: `apps/api/src/controllers/rate-limit.controller.ts:20-21`, `apps/api/src/controllers/rate-limit.controller.ts:155-177`, `apps/api/src/app.module.ts:110-123` -> Action: Gate clear/test/stats endpoints behind `AdminAuthGuard` and/or remove them from production module registration. `DELETE /rate-limit/clear` can clear arbitrary identifiers.
* **Dependency audit currently fails with critical/high vulnerabilities** - Evidence: `npm audit --workspaces --audit-level=moderate` reported critical `handlebars` and `shell-quote`, high `axios`, `@grpc/grpc-js`, `fast-uri`, `fast-xml-parser`, `flatted`, `form-data`, `multer` via `@nestjs/platform-express`, `protobufjs`, `undici`, and `ws`. -> Action: Run a dedicated dependency remediation pass using package-manager upgrades/overrides, then rerun the workspace audit.

## 🐞 Bugs & Logic Issues
* **Edit-post image replacement stores device-local picker URIs in the API** - File: `apps/mobile/components/Feed/EditPostModal.tsx:63-75`, `apps/mobile/components/Feed/EditPostModal.tsx:117-121`, `apps/mobile/components/PostsReelsScreen.tsx:564-568`, `apps/mobile/utils/postsService.ts:342-350`, `apps/api/src/controllers/posts.controller.ts:2349-2356` -> Recommendation: Upload `file://`, `content://`, or `blob:` images through the existing storage service before calling `PUT /api/posts/:postId`; persist only public HTTPS storage URLs. This recent flow currently breaks images for other users/devices.
* **`PUT /api/posts/:postId` collapses multi-image posts to one image** - File: `apps/api/src/controllers/posts.controller.ts:2275-2282`, `apps/api/src/controllers/posts.controller.ts:2349-2356` -> Recommendation: Accept an `images: string[]` contract or replace only the edited image index. The current single `image` field overwrites the whole `posts.images` array.
* **Admin task infinite scroll can skip a page under rapid `onEndReached` calls** - File: `apps/mobile/screens/admin/useAdminTasksScreen.tsx:220-225`, `apps/mobile/screens/admin/useAdminTasksScreen.tsx:352-409`, `apps/mobile/screens/admin/useAdminTasksScreen.tsx:411-426` -> Recommendation: Add a `loadingMore` guard/state around load-more fetches. `loading` is false during pagination, so repeated load-more calls can advance `page` before the prior request completes; the sequence guard then drops intermediate responses.
* **Hidden posts remain fetchable by direct id** - File: `apps/api/src/controllers/posts.controller.ts:754`, `apps/api/src/controllers/posts.controller.ts:1937-1945` -> Recommendation: Apply the same `status != 'hidden'` visibility predicate to `GET /api/posts/:postId`, with explicit owner/admin override if required.
* **Friends feed scope trusts a query-string viewer id** - File: `apps/api/src/controllers/posts.controller.ts:776-782`, `apps/api/src/controllers/posts.controller.ts:830-840` -> Recommendation: Require auth for `feed_scope=friends`, derive the viewer from the token, and ignore client-provided `user_id`.
* **Items and Trump search-mode filter chips are partially no-ops** - File: `apps/mobile/donationScreens/items/itemsScreenFiltering.ts:101-112`, `apps/mobile/donationScreens/items/itemsScreenFiltering.ts:179-205`, `apps/mobile/donationScreens/trump/getFilteredRidesForTrumpScreen.ts:33-41`, `apps/mobile/donationScreens/trump/getFilteredRidesForTrumpScreen.ts:80-95` -> Recommendation: Port condition/delivery/tag and smoking/pets/kids matching into search mode or hide unsupported chips. Current UI presents filters that do not affect results.

## 🏗️ Architecture & Clean Code
* **Clean Architecture target is not yet enforced by folder structure** - Feedback: `docs/SSOT/CODE_QUALITY/layers-and-boundaries.md` defines Domain/Application/Infrastructure boundaries, but the repo is still legacy-shaped. No high-confidence classic Domain-to-DB import was found in extracted pure helpers reviewed (`feedFilters`, items/trump filtering, admin task utils, task validation), but controllers and mobile utilities still mix orchestration and infrastructure.
* **Post edit image handling crosses UI/orchestration/storage responsibilities** - Feedback: `EditPostModal` collects a picker URI, `PostsReelsScreen` directly calls `postsService`, and the API treats the string as a persisted storage URL. Move upload orchestration to a narrow application/service layer and keep the modal presentation-only.
* **Domain-like mapper imports infrastructure logging** - File: `apps/mobile/donationScreens/items/mapPostToFeedItemForItemsScreen.ts:1-4` -> Action: Keep post-to-feed mappers pure; inject logging at the call site or use a minimal logger port if parse failures must be observed.
* **Admin file read endpoints are weaker than write endpoints** - File: `apps/api/src/controllers/admin-files.controller.ts:80-128`, `apps/api/src/controllers/admin-files.controller.ts:142-204` -> Action: SRS currently lists GET routes as `JwtAuthGuard`, but security posture is inconsistent with admin folder/file metadata. Revisit product policy and consider `AdminAuthGuard` for reads as well.

## 🧹 Technical Debt & Maintenance
* **Supply-chain monitoring gap after Snyk removal** - Files: `package-lock.json`, `package.json` -> Action: Replace the removed Snyk gate with a maintained dependency-audit workflow or equivalent advisory monitoring. Today `npm audit` fails and there is no committed remediation gate visible in this run.
* **Large legacy surfaces remain high-risk change zones** - Files: `apps/api/src/controllers/posts.controller.ts`, `apps/mobile/utils/databaseService.ts` -> Action: Continue extracting cohesive services/use cases around post interactions, visibility, and mobile data access. The post controller contains security-sensitive auth, SQL, visibility, and mutation logic in one large file.
* **Duplicate post-to-feed mapping paths risk divergent feed behavior** - Files: `apps/mobile/utils/mapApiPostToFeedItem.ts`, `apps/mobile/donationScreens/items/mapPostToFeedItemForItemsScreen.ts`, `apps/mobile/donationScreens/trump/mapPostToFeedItemForTrumpScreen.ts` -> Action: Extract a shared core mapper and screen-specific extension hooks.
* **Admin task validation exists in both mobile and API with drift risk** - Files: `apps/mobile/screens/admin/adminTasksScreen.utils.ts`, `apps/api/src/tasks/tasks-validation.ts` -> Action: Share task validation contracts or add parity tests for estimated hours, due dates, and request-body normalization.
* **Security scan notes** - Evidence: repository-wide pattern scans found no private-key blocks, GitHub personal access tokens, AWS access-key IDs, or OpenAI-style `sk-*` keys. The only confirmed hardcoded credential from the scan is the Railway development JWT secret above.
