/**
 * Organization root manager identity — must stay aligned with API
 * `ROOT_ADMIN_EMAIL` / `KC_ROOT_ADMIN_EMAIL_FALLBACK` (`apps/api/src/config/kc-root-admin.defaults.ts`).
 * Use this for org-facing contact and the only client-side hardcoded root fallback.
 */
export const KC_ORGANIZATION_ROOT_EMAIL = "karmacommunity2.0@gmail.com";
